# 护符系统 —— HK 装配界面逻辑梳理 + 素材清单

> 来源：`reference/HK/Assembly-CSharp/Assembly-CSharp` 反编译源码 + HK 游戏资源。
> 目标：在 AIC 模组里为小骑士实现护符系统，装配界面参照 HK 原版逻辑。

## 一、HK 原版护符装配界面逻辑

### 界面结构
HK 的护符装配界面是一个名为 **“Charms Pane”** 的 UI 面板（Tag: `Charms Pane`），
交互状态机是挂在它上面的 PlayMaker FSM **“UI Charms”**（核心交互逻辑在 FSM 里，
C# 侧只有钩子与数据）。界面分两块：

1. **上部：护符库存（网格）**
   - 每个格子 = `InvCharmBackboard`（背景板）+ 一个护符图标。
   - 背景板有 `blankSprite`（未获得/空格）与 `activeSprite`（已获得的格子）。
   - 新获得的护符格子上显示 `newOrb`（小圆点，选中后 `SimpleFadeOut` 淡出）。
   - 每个格子绑 `charmNum` / `charmNumString` / `gotCharmString` / `newCharmString`
     （对应 PlayerData 的 `gotCharm_XX` / `newCharm_XX` 布尔）。
2. **下部：装备栏（横向一排）**
   - `BuildEquippedCharms` 按 `PlayerData.equippedCharms`（List&lt;int&gt;）顺序实例化护符图标：
     `START_X=-7.28, START_Y=-3.86, CHARM_SCALE=1.15, 间距 1.76`（>10 个时收窄）。
   - 末尾显示 `nextDot`（下一个空槽提示点）。
   - `UpdateNotches()`（槽位点数刷新，原版在 FSM 里做）。

### 护符图标（CharmDisplay / CharmIconList）
- `CharmDisplay` 挂在每个护符图标上：按 `id` 从 `CharmIconList.spriteList[id]` 取图；
  特殊 id 走变体：
  - 23/24/25 脆弱护符碎裂 → `brokenGlassHP/Geo/Attack`
  - 36 虚空之心 → `whiteCharm` / `blackCharm`（按 `royalCharmState`）
  - 40 格林之子 → `grimmchildLevel1..4` / `nymmCharm`
- 装配成功时：FSM 写 “Newly Equipped Name”，`Check()` 检测到后播放
  `charmPlaceEffect` 并点亮 `flashSpriteRenderer`（闪光层）。
- 超载（`overcharmed`）时图标抖动。

### 交互流程（FSM “UI Charms” + C# 钩子）
1. 选中库存格子 → `SelectCharmBackboard` → 清 `newCharm`（新护符圆点淡出）。
2. 选择护符 → 检查槽位 → 装备/卸下 → 更新 `equippedCharms`、`charmSlotsFilled`。
3. 成功 → 播放装配特效 + 闪光；失败（槽位不足）→ 失败反馈；超载 → 特殊反馈。
4. 槽位点数：`charmSlots`（上限 11）/ `charmSlotsFilled` + 已装备护符 cost 总和。

### 关键数据（PlayerData）
- `gotCharm_1..39`：是否拥有（`gotCharm_36` 不存在，36 用 `royalCharmState`）。
- `equippedCharms`：装备中的护符 id 列表（顺序 = 装备栏顺序）。
- `charmSlots`（上限 11）/ `charmSlotsFilled` / `overcharmed`。
- `charmCost_1..40`：各护符槽位消耗（见下表）。
- `brokenCharm_23/24/25`、`royalCharmState`。

## 二、素材清单（请从 HK 资源提取）

> 提取工具建议：AssetStudio 打开 `hollow_knight_Data` 下的
> `globalgamemanagers.assets` / `level*.assets`，按类型（Sprite/Texture2D/TextAsset/GameObject/Prefab）
> 筛选下面列出的名字。护符装配界面场景名一般为 `Charm_Inventory`（含 Charms Pane）。

### A. 护符图标（必须，40 张 + 变体）
| 素材 | 数量 | 说明 |
|---|---|---|
| `Charm_Icon_01` … `Charm_Icon_40` | 40 | 护符图标本体（透明 PNG，建议统一尺寸如 64×64，含原始尺寸供参考） |
| Unbreakable Heart / Greed / Strength | 3 | id 23/24/25 的强化版图标 |
| Grimmchild Level 1..4 + Nymm | 5 | id 40 的等级变体（如不做召唤护符可跳过） |
| Void Heart（白）/（黑） | 2 | id 36 虚空之心两种状态 |
| Broken Glass HP / Geo / Attack | 3 | 碎裂版脆弱护符（如不做碎裂机制可跳过） |

### B. 装配界面 UI（按需）
| 素材 | 说明 |
|---|---|
| 库存格子背景（空格 + 有护符） | `InvCharmBackboard` 的 blank/active 两张背景 |
| newOrb（新护符圆点） | 新获得指示点（含淡出动画帧） |
| nextDot（空槽提示点） | 装备栏下一个空槽的提示 |
| charmPlaceEffect | 装配护符时的粒子/特效（Prefab 或贴图帧） |
| Flash Sprite（装配闪光层） | 与护符图标同形的闪光贴图 |
| 界面背景图 | HK 护符界面整体背景（如要复刻观感） |
| notch 点数指示 | 槽位点数 UI（满/空/超载配色） |

### C. 数据（非图片）
- 40 个护符的**名字 / 描述 / 槽位消耗**（名字与描述在 HK 语言文件里，cost 见下表）。
- 需要确定：本模组首批做**哪几个护符、各护符效果**（后续工程，但素材按全量提取备用）。

### D. 音效（可选，后补）
- 装配成功 / 失败 / 超载音效（HK 原版护符界面的音效）。

## 三、护符 id ↔ 槽位消耗（PlayerData）
| id | cost | id | cost | id | cost | id | cost |
|---|---|---|---|---|---|---|---|
| 1 | 1 | 11 | 3 | 21 | 4 | 31 | 2 |
| 2 | 1 | 12 | 1 | 22 | 2 | 32 | 3 |
| 3 | 1 | 13 | 3 | 23 | 2 | 33 | 2 |
| 4 | 2 | 14 | 1 | 24 | 2 | 34 | 4 |
| 5 | 2 | 15 | 2 | 25 | 3 | 35 | 3 |
| 6 | 2 | 16 | 2 | 26 | 1 | 36 | 0/5* |
| 7 | 3 | 17 | 1 | 27 | 4 | 37 | 1 |
| 8 | 2 | 18 | 2 | 28 | 2 | 38 | 3 |
| 9 | 3 | 19 | 3 | 29 | 4 | 39 | 2 |
| 10 | 1 | 20 | 2 | 30 | 1 | 40 | 2 |

*36 虚空之心不占槽位（cost=0，解锁后替代旧国王徽章）。

## 四、已提取素材（2026-08-21，已放入 `assets/hk/sheets/charms/`）

> 提取工具：`tools/HKCharmExporter`（读取 `D:\Documents\GAME\Hollow Knight\hollow_knight_Data`，
> 输出到 charms 目录的 `FX/` `UI/` `Text/` `Audio/` 子目录）。

### 1. 特效 FX/
- `UI_overcharm_crack_effects_0004_Layer-13.png`（670x213）
- `UI_overcharm_crack_effects_0005_Layer-11.png`（479x394）
- `UI_overcharm_crack_effects_0006_Layer-12.png`（366x255）
- `overcharm_shatter_particle.png`（66x250，超载碎裂粒子，**新增**）

> 注意：前 3 张就是用户已提取的 `Overcharm/overcharm_1/2/3.png`（尺寸一一对应），
> 原版中为 3 层裂纹叠加动画，无需重复使用。

### 2. 装配界面 UI/
- 背景板：`charm_backboard`（68x68）、`overcharm_backboard`（746x165）、
  `menu_backboard`（10x10 纯白底，用于整体着色）、`Menu_Border_Black`（512x305）
- 槽位点数：`charm_cost`（38x38）、`charm_cost_full`/`charm_cost_empty`（32x32）、
  `charm_UI__0000_charm_cost_02_lit`（48x46）/`_0001_..._unlit`（28x28）、`Charm_Notch`（119x105）
- 新护符圆点：`new_item_orb`（49x49）
- 提示：`Charms_prompt`（454x338，进界面提示）、`GG_bound_charm_prompt`（152x156）
- 护符商人相关：`charm_slug`、`pin_charm_slug`
- 特殊图标变体：`charm_white_left/right/full`、`charm_black`、`glass_charm_shattered_UI`、
  `charm_fluke`、`charm_hive`、`charm_crystal`

### 3. 文本 Text/
- `ZH_UI.txt` / `EN_UI.txt` / `JP_UI.txt`：**护符名称/描述/界面标签**（`CHARM_NAME_x`、
  `CHARM_DESC_x`、`CHARM_TXT_COST`、`PANE_CHARMS` 等），做装配界面直接可用。
- `ZH_Prompts.txt` / `EN_Prompts.txt` / `JP_Prompts.txt`：界面提示文本。
- 16 个 `<语言>_Charm Slug.txt`：萨鲁巴商店对话（非护符数据，备用）。
- 5 个 `.bin`（charm_place / charm_place_fail / charm_place_overcharm /
  overcharm_unlock_hit / overcharm_unlock_final_hit）：二进制 FMOD 事件键数据，
  无法直接当文本用；实际音效用 Audio/ 下的 wav。

### 4. 音效 Audio/（wav，已解码）
- `charm_click_in.wav`（0.04s，原版护符装备“咔哒”声，最可能是装配音效）
- `ui_change_selection.wav`（0.35s，菜单切换）、`ui_button_confirm.wav`（2.34s，确认）、
  `ui_option_click.wav`（0.05s，选项点击）、`ui_save.wav`、`ui_map_update.wav`
- 护符能力音效：`hero_fury_charm_loop/burst`（亡者之怒）、`hero_blocker_charm_block`（坚硬外壳）、
  `hero_shroom_charm_poof`（蘑菇孢子）

> HK 游戏内不存在名为 charm_place 的独立 AudioClip（那些 TextAsset 只是 FMOD 事件键），
> 原版 UI 音频实际走 Unity AudioClip，已全部导出为 wav。

## 五、槽位（凹槽）素材与 HK 原版界面的对应关系（已从 level1 场景验证）

> 验证方法：解析 HK `level1` 场景里所有 SpriteRenderer 的 m_Sprite 引用 + 场景对象层级，
> 结果文件：`assets/hk/sheets/charms/asset_list_sr2.txt`、`asset_list_go_comp.txt`。

### 1. 用户 Groove/ 文件夹的真实身份（与原版 Sprite 一一对应，MD5 相同）

| 用户文件名 | 原版 Sprite | 尺寸 | 原版用途 |
|---|---|---|---|
| `Groove/cost_white.png` | `charm_UI__0000_charm_cost_02_lit` | 48×46 | **已占用槽位的圆点（点亮）**。场景对象 `Cost 1..6`、`Over 1..6`、`Sprite Full` 都用它 |
| `Groove/cost_black.png` | `charm_UI__0001_charm_cost_02_unlit` | 28×28 | **空槽位圆点（熄灭）**。场景对象 `Sprite Empty` |
| `Groove/charm_up.png` | `charm_cost` | 38×38 | **装备栏末尾“下一个空槽”提示点**（`BuildEquippedCharms.nextDot`，场景对象 `Sprite Next`） |
| `Groove/charm_down.png` | `charm_backboard` | 68×68 | **护符格/装备栏的背景板**（场景对象 `BB 1..40` 全部使用） |

### 2. UI/ 目录里槽位相关素材

| 文件 | 原版 Sprite | 尺寸 | 说明 |
|---|---|---|---|
| `UI/charm_cost_full.png` | `charm_cost_full` | 32×32 | 全游戏 SpriteRenderer 无静态挂载，推测由 FSM 动态赋给 12 槽指示行（`Charm Cost 1..12`） |
| `UI/charm_cost_empty.png` | `charm_cost_empty` | 32×32 | 同上（空槽态） |
| `UI/Charm_Notch.png` | `Charm_Notch` | 119×105 | 萨鲁巴商店“护符槽”商品图标（sharedassets10 `Item Sprite`） |
| `UI/overcharm_backboard.png` | `overcharm_backboard` | 746×165 | 超载背板（`OC Backboard`），与用户 Overcharm/overcharm_backboard 相同 |
| `UI/menu_backboard.png` | `menu_backboard` | 10×10 | 整面板底色（拉伸着色），`backboard` 对象 |

### 3. 原版界面结构（level1 场景，护符菜单）

- **库存**：`Backboards` 容器 → `BB 1..40`（`charm_backboard`）+ 护符图标（Inv_0010..0013 图集块）。
- **装备栏**：`Equipped Charms` 容器 → 护符图标排 + `Sprite Next`（`charm_cost`）。
- **槽位指示**：`Notches` + `Text Notches`（TMPro，文案键 `CHARM_TXT_NOTCHES`=“凹槽”）
  与 `Charm Cost 1..12`（12 个 FSM 驱动对象）；费用圆点 `Cost 1..6`（lit 默认，FSM 切 unlit/染色）
  与 `Over 1..6`（超载态，`notchOverColor` 染红）。
- **超载**：`OC Backboard`（overcharm_backboard）+ 3 层裂纹
  （`crack_big`=0004、`UI_overcharm_crack_effects_0005`、`0006`）+ `Overcharm_Break` 碎裂粒子 + `Text Overcharmed`。

### 4. 相关本地化键（ZH_UI）

`CHARM_TXT_NOTCHES`=凹槽、`CHARM_TXT_COST`=花费、`CHARM_TXT_NOTCHCOST`=需要凹槽、
`CHARM_TXT_OVERCHARMED`=护符过载、`CHARM_TXT_EQUIPPED`=已装备、`INV_NAME_NOTCH`=护符槽。

---

## 六、诺艾尔的护符界面（本模组实现，2026-09-22）

> 本节是"模组侧"的记录，不是 HK 原版素材清单；放在这里是因为它与上面第二节的护符图标、
> 第四节已提取的 `sheets/charms/` 素材、第五节槽位素材是同一套资源。

### 1. 目标与当前进度

| 部分 | 内容 | 状态 |
|---|---|---|
| 第一部分 | 诺艾尔能打开护符界面、装配/卸下护符；**诺艾尔与小骑士的护符互相独立** | ✅ 已完成（build=2026-09-22.2） |
| 第二部分 | 诺艾尔的护符**产生效果** | 🔄 进行中：**1 任性的指南针、2 蜂群集结已完成**（build=2026-09-22.8；蜂群集结的魔力草为"不掉球、每棵直接回诺艾尔 30 MP"，见 KIC 文档 21.7 / 21.8），其余护符待做 |
| 第三部分 | "事件"式解锁（完成任务解锁护符、按开启宝箱数提升槽位上限） | ⏳ 待做 |

详细设计与踩坑见 `docs/KIC_核心机制与实现总结.md` 第 20 节。

### 2. 素材结论：**不需要新增任何素材**

- 界面本身直接复用现成的一套：`charm_ui/layout.json + images/*.png`（运行时从插件目录
  `plugins/KnightInCradle/charm_ui/` 读取，由 `CharmUiLoader` 建 UGUI Canvas），**两套护符共用同一个界面对象**，
  只是"当前编辑谁"不同（`CharmUiController.Owner`）。
- 护符图标/特效/音效全部沿用第四节已提取的 `assets/hk/sheets/charms/`（`Charms/`、`FX/`、`Audio/`…），
  与护符 id 的对应关系见第三、四节。
- 诺艾尔能装的护符**与小骑士完全一致**（`CharmDatabase` 全表、11 槽），因此没有"诺艾尔专用图标"要补。
  将来若加诺艾尔专属护符，再按第二节 A 的方式补图标，并在 `CharmDatabase` 里登记。

### 3. 与小骑士侧的差异（实现层）

| 项目 | 小骑士 | 诺艾尔 |
|---|---|---|
| 打开方式 | 随时（骑士模式）按 `O`（可配置） | 随时（诺艾尔模式）按 `O` |
| 装配/卸下条件 | 必须坐在长椅上（`KnightEntity.IsSitting`） | 必须坐在长椅上（`PRNoel.isBenchState()`，即 AIC 原生长椅状态） |
| 存档命名空间 | `kic_charm_slot0..10`（旧格式，保持不变） | `kic_noel_charm_slot0..10`（新增） |
| 格林之子↔无忧旋律 变体键 | `kic_charm_variant` | `kic_noel_charm_variant`（新增） |
| 虚空之心（id 40） | 恒在已装备栏首位、不可卸下 | **不出现在界面里**（按需求去掉） |
| 束缚（寻神者自限 `kic_gg_*`） | 可用（sign 点击流程） | 不参与（sign 点击对诺艾尔无效） |
| 坚固贪婪背包容量 / 装配时血量联动 | 生效 | 不参与（第二部分再定） |
| 与原生长椅菜单 | 小骑士的坐姿是模组自管，不弹原生长椅菜单 | AIC 原生长椅菜单会弹出，护符界面**叠加**在其上，输入被 `CharmUiInputPatch` 冻结，关闭后继续用原菜单 |

### 4. 相关代码

| 文件 | 职责 |
|---|---|
| `src/CharmUi/CharmSave.cs` | 两套归属的装备快照与存档命名空间（`CharmOwner { Knight, Noel }`） |
| `src/CharmUi/CharmUiController.cs` | `Owner` / `SetOwner()`、装配装卸、变体切换、槽位与超载判定 |
| `src/CharmUi/CharmUiOnGuiLayer.cs` | 界面绘制（对两套归属通用，含槽位底图、超载裂纹、飞行装配动画） |
| `src/CharmUi/CharmEffects.cs` | 护符效果（目前只服务小骑士；第二部分加诺艾尔视角的查询入口） |
| `src/KnightInCradleBehaviour.cs` | `ToggleCharmUi()`：按模式决定归属与坐姿来源；`EnsureCharmUiCreated()` 共用一个界面对象 |

### 5. 测试要点（素材相关）

1. 护符界面打开后，图标、槽位底图、超载裂纹、装配动画都应与小骑士界面**完全一致**（同一套素材）；
2. 若诺艾尔界面出现"图标空白/槽位缺失"，先查 `plugins/KnightInCradle/charm_ui/images/`（界面素材）
   与 `plugins/KnightInCradle/assets/hk/sheets/charms/`（护符图标）是否完整——
   两套护符共用这些文件，不会因为"是诺艾尔"而少图。

### 6. `layout.json` 里几个"看着多余、其实不能删"的静态元素

界面是按 Unity 工程导出的 `charm_ui/layout.json` 重建的，其中这几个元素**不是**网格里的护符图标，
而是被代码当作"位置基准/固定图标"使用；诺艾尔侧由代码主动隐藏，但**重新导出布局时必须保留**：

| 元素路径 | 图片 | 位置 | 作用 |
|---|---|---|---|
| `CharmUi/Charms/Image (39)` | `40_VOID.png` | (-800, 252) | **虚空之心在已装备栏的图标**（小骑士侧用；诺艾尔侧被 `IsIconHidden` 隐藏） |
| `CharmUi/Equipment/charm_up (1)` | `charm_up.png` | (-800, 245) | 装备栏槽位 **1 号样例**：`GetBaseSlotRects()` 用它取槽位位置，`DrawEquippedBar()` 用它算起点 |
| `CharmUi/Equipment/charm_up (2)` | `charm_up_2.png` | (-690, 245) | 装备栏槽位 **2 号样例**：与 1 号的 x 差给出**槽位间距**（110px） |
| `CharmUi/Equipment/charm_up_copy` | `charm_up_2_2.png` | — | tag = `charm_up_template`：动态槽位绘制用的模板贴图（模板标签的元素不会静态绘制） |

> 诺艾尔侧的表现：虚空之心不出现在任何位置；空装备栏显示 1 个空槽（第一个护符的落点），
> 装了 N 个护符就显示 N+1 个槽。细节见 `docs/KIC_核心机制与实现总结.md` 第 20.8 节。
